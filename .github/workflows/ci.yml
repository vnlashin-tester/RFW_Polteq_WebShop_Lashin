name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # Обновлено до версии, использующей Node.js 20

    - name: Set up Python
      uses: actions/setup-python@v4  # Обновлено до версии, использующей Node.js 20
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m venv .venv
        source .venv/bin/activate
        pip install -r requirements.txt

    - name: Run tests
      run: |
        chmod +x run_tests.sh
        ./run_tests.sh
